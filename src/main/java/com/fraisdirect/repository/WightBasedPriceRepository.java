package com.fraisdirect.repository;

import com.fraisdirect.entity.WightBasedPrice;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WightBasedPriceRepository extends JpaRepository<WightBasedPrice,Long> {
}
